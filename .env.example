# Render / Local environment template
# Fill these in Render Dashboard (Environment) or copy to .env for local dev

PORT=5000
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:8888

# Database (MySQL)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=15m

# SMTP (for sending OTP emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@example.com
